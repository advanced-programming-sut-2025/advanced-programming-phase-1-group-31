package view;

import controller.HouseMenuController;

import java.util.Scanner;

public class HouseMenu implements Menu{
    HouseMenuController controller = new HouseMenuController();
    public void checkCommand(Scanner scanner){    
        if (controller.run(scanner).message() 1= null){

            System.out.println(controller.run().message());
        }  
    }
}
 
    public Result run(Scanner scanner) {
        return new Result(true, null);
            String input = scanner.nextLine().trim();
            Matcher matcher;

            if ((matcher = HouseMenuCommand.COOKING_REF_PUT_PICK.getMatcher(input)) != null) {
                return function(matcher);
            }   
}
private Result function(Matcher matcher){
        
    }