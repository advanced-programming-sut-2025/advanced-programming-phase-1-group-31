package view;

import java.util.Scanner;

public class ExitMenu implements Menu{
    public void checkCommand(Scanner scanner){
    }
}
//all the game's data must be uploaded to a file when exitmenu is called
